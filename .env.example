# Supabase connection (get these from your Supabase project settings → API)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase service role key (for serverless functions — bypasses RLS)
# Find this in Supabase dashboard → Settings → API → service_role key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Monday.com API (OKR "Push to Monday" integration — server-side only)
# Get a token: Monday → your avatar (bottom-left) → Developers → My Access Tokens
# (or Admin → API). The token must have write access to the SEO Project
# Management board (1873344869) and its subitems.
MONDAY_API_TOKEN=your-monday-personal-api-token
